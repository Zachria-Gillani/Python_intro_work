i = 0
#Above I am just setting the random variable i to 0
numbers = []
#Above i am just starting an open list with the name random name numbers

while i < 6:
    print(f"At the top is {i}")
    numbers.append(i)
#Above is the start of the while loop. I am taking an open list and adding 0 to up to 6
#This can be seen by the boolean arithmatic while i < 6.
    i = i + 1
    #without this piece of code, the while loop would never ened and when I run it in the shell it would just print 0 a whole lot
    #This assigns i to an increasing incremental value of 1 every time wthe while loop runs
    print("Numbers now:", numbers)
    print(f"At the bottom i is {i}")

#The whole block of code above this is just going to print those statments off in alternating fashion up until 6
print("The numbers:")

for num in numbers:
    print(num)

    #above code block is a for loop that will simply list numbers vertically
#below I have made the above while loop as a function...well I found it online at least
def fun_name():
    i = 0
    numbers = []
    while i < 6:
        print "At the top of i is %d" % i
        numbers.append(i)
        i = i + 1
    print "Numbers now: ", numbers
    print "At the bottom i is %d" % i

    print "The numbers: "

    for num in numbers:
        print num
