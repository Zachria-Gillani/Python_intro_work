def add(a,b):
    print(f"ADDING {a} + {b}")
    return a + b

    #The return function allows for me to unassign variables.  Meaning I can use them for mutiple variables down the road.  Without
    #Having return here, my answer at the bottom would not print out a number.

    #Overall, here I am just adding definitions of basic arithmatic functions

def subtract(a, b):
    print(f"SUBTRACTING {a} - {b}")
    return a - b

def multiply(a, b):
    print(f"MULTIPLYING {a} * {b}")
    return a * b

def divide(a,b):
    print(f"DIVIDING {a} / {b}")
    return a / b

    #The {a} and {b} with the f string allows for my function to be present with the later assigned variables for a and b


print("Let's do some math with just functions!")

age = add(30,5)
height = subtract(78, 4)
weight = multiply(90, 2)
iq = divide(100,2)

#The above lines of code (4 of them) allow me to call my definitions and add into them the numbers for the a and b variables.
#This means the previous code was all a set up and now I am plugging in those numbers into those functions I made with the print statements.

print(f"Age {age}, Hight: {height}, Weight: {weight}, IQ: {iq}")

#Here I am calling a print with function and now my functions are going to have the function applied to the variables now associated with them


#A puzzle for the extra credit, type it in anyway
print("here is a puzzle.")

what = add(age, subtract(height, multiply(weight, divide(iq, 2))))

print("That becomes:", what, "Can you do it by hand?")
